// 실시간 비디오 출력: 캔버스 사용

let video;

function setup() {
  createCanvas(400, 300);
  video = createCapture(VIDEO);
  video.hide();	       // 비디오 요소 숨김
}

function draw() {
  background(0);
  image(video, 0, 0);
}